import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Fix = () => {
  return (
    <View>
      <Text>Fix</Text>
    </View>
  );
};

export default Fix;

const styles = StyleSheet.create({});
