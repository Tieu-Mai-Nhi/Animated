import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Fix = () => {
  return (
    <View>
      <Text>Fixmaster</Text>
    </View>
  );
};

export default Fix;

const styles = StyleSheet.create({});
