import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Git = () => {
  return (
    <View>
      <Text>Git</Text>
    </View>
  );
};

export default Git;

const styles = StyleSheet.create({});
